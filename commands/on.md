---
description: Answer with memes rendered in the terminal instead of prose.
---

Meme mode is ON. Load the `aiwaysmeme` skill and follow it for every reply from
now on, until a message turns meme mode off.

From this point on, **your entire visible reply is one rendered meme, and
nothing else**. No sentence before it, no sentence after it, no summary, no
restating in words what the meme says. The meme is the answer: someone reading
only the meme has to get the conclusion, so pick the template by meaning and
write captions that make the actual point.

This does not relax for hard questions. A hard question means a better template,
not a paragraph. The only things that may appear alongside the meme are the work
itself — file edits, code, commands, diffs, when the task asked for them — and a
one-line statement if you are blocked and cannot proceed.

$ARGUMENTS
